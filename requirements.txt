# Runtime and test dependencies, kept to what the harness actually imports.
#
# This file was previously a catalogue of an "AI stack" -- LangChain, LangGraph,
# Qdrant, sentence-transformers, Kafka, gRPC, boto3 -- none of which any module
# here imports. It made `pip install -r requirements.txt` a multi-hundred-megabyte
# download (sentence-transformers alone pulls in torch) before a reader could run
# a single test. Anything reinstated below should be something a file imports.

# --- test runner -------------------------------------------------------------
pytest==8.3.4
allure-pytest==2.13.5             # api_core.utils.* decorate steps with @allure.step

# --- models and validation ---------------------------------------------------
pydantic==2.10.3                  # api_core/models/*

# --- HTTP clients ------------------------------------------------------------
httpx==0.28.1                     # api_core core + validators
requests==2.32.3                  # tests/demo fixtures

# --- oracles -----------------------------------------------------------------
sqlalchemy==2.0.36                # api_core/utils/db_utils.py
psycopg2-binary==2.9.10           # database oracle driver
redis==5.2.1                      # api_core/utils/redis_utils.py

# --- lint --------------------------------------------------------------------
ruff==0.13.2                      # pinned: an unpinned linter turns a new ruff
                                  # release into a red CI on an unchanged repo
